{
  "name": {
    "required": true,
    "rules": [
      "required"
    ],
    "messages": [
      "الحقل الاسم مطلوب.",
      "يجب أن لا يتجاوز طول نص الاسم 20 حروفٍ/حرفًا"
    ]
  },
  "country_id": {
    "required": true,
    "rules": [
      "required"
    ],
    "messages": [
      "الحقل country id مطلوب."
    ]
  },
  "currency_id": {
    "required": true,
    "rules": [
      "required"
    ],
    "messages": [
      "الحقل currency id مطلوب."
    ]
  },
  "city_id": {
    "required": true,
    "rules": [
      "required"
    ],
    "messages": [
      "الحقل city id مطلوب."
    ]
  },
  "district_id": {
    "required": true,
    "rules": [
      "required"
    ],
    "messages": [
      "الحقل district id مطلوب."
    ]
  },
  "rf_community_id": {
    "required": true,
    "rules": [
      "required"
    ],
    "messages": [
      "الحقل rf community id مطلوب."
    ]
  },
  "category_id": {
    "required": true,
    "rules": [
      "required"
    ],
    "messages": [
      "الحقل category id مطلوب."
    ]
  },
  "type_id": {
    "required": true,
    "rules": [
      "required"
    ],
    "messages": [
      "الحقل type id مطلوب."
    ]
  },
  "name_ar": {
    "required": true,
    "rules": [
      "required"
    ],
    "messages": [
      "الحقل name ar مطلوب."
    ]
  },
  "name_en": {
    "required": true,
    "rules": [
      "required"
    ],
    "messages": [
      "الحقل name en مطلوب."
    ]
  },
  "days": {
    "required": true,
    "rules": [
      "required"
    ],
    "messages": [
      "الحقل days مطلوب."
    ]
  },
  "booking_type": {
    "required": true,
    "rules": [
      "required"
    ],
    "messages": [
      "الحقل booking type مطلوب."
    ]
  },
  "complex_id": {
    "required": true,
    "rules": [
      "required"
    ],
    "messages": [
      "الحقل complex id مطلوب."
    ]
  },
  "gender": {
    "required": true,
    "rules": [
      "required"
    ],
    "messages": [
      "الحقل النوع مطلوب."
    ]
  },
  "approved": {
    "required": true,
    "rules": [
      "required"
    ],
    "messages": [
      "الحقل approved مطلوب."
    ]
  }
}