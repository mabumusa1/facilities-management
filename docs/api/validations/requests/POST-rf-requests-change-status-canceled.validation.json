{
  "endpoint": "rf/requests/change-status/canceled",
  "method": "POST",
  "fields": {
    "rf_request_id": {
      "field": "rf_request_id",
      "required": true,
      "type": "string",
      "rules": [
        {
          "rule": "required",
          "rawMessage": "الحقل rf request id مطلوب."
        }
      ],
      "examples": {
        "valid": [
          "example"
        ],
        "invalid": [
          "",
          null,
          null
        ]
      }
    }
  },
  "requiredFields": [
    "rf_request_id"
  ],
  "optionalFields": []
}