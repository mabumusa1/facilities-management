{
  "endpoint": "rf/requests/types/{id}",
  "method": "PUT",
  "fields": {
    "name_ar": {
      "field": "name_ar",
      "required": true,
      "type": "string",
      "rules": [
        {
          "rule": "required",
          "rawMessage": "الحقل name ar مطلوب."
        }
      ],
      "examples": {
        "valid": [
          "example"
        ],
        "invalid": [
          "",
          null,
          null
        ]
      }
    },
    "name_en": {
      "field": "name_en",
      "required": true,
      "type": "string",
      "rules": [
        {
          "rule": "required",
          "rawMessage": "الحقل name en مطلوب."
        }
      ],
      "examples": {
        "valid": [
          "example"
        ],
        "invalid": [
          "",
          null,
          null
        ]
      }
    },
    "rf_sub_category_id": {
      "field": "rf_sub_category_id",
      "required": true,
      "type": "string",
      "rules": [
        {
          "rule": "required",
          "rawMessage": "الحقل rf sub category id مطلوب."
        }
      ],
      "examples": {
        "valid": [
          "example"
        ],
        "invalid": [
          "",
          null,
          null
        ]
      }
    },
    "icon": {
      "field": "icon",
      "required": false,
      "type": "string",
      "rules": [
        {
          "rule": "unknown",
          "rawMessage": "يرجى اختيار أيقونة لهذا النوع من الخدمات."
        }
      ],
      "examples": {
        "valid": [
          "example"
        ],
        "invalid": [
          "",
          null
        ]
      }
    },
    "fee_type": {
      "field": "fee_type",
      "required": true,
      "type": "string",
      "rules": [
        {
          "rule": "required",
          "rawMessage": "الحقل fee type مطلوب."
        }
      ],
      "examples": {
        "valid": [
          "example"
        ],
        "invalid": [
          "",
          null,
          null
        ]
      }
    }
  },
  "requiredFields": [
    "fee_type",
    "name_ar",
    "name_en",
    "rf_sub_category_id"
  ],
  "optionalFields": [
    "icon"
  ]
}