{
  "endpoint": "rf/requests/sub-categories",
  "method": "POST",
  "fields": {
    "name_ar": {
      "field": "name_ar",
      "required": true,
      "type": "string",
      "rules": [
        {
          "rule": "required",
          "rawMessage": "الحقل name ar مطلوب."
        }
      ],
      "examples": {
        "valid": [
          "example"
        ],
        "invalid": [
          "",
          null,
          null
        ]
      }
    },
    "name_en": {
      "field": "name_en",
      "required": true,
      "type": "string",
      "rules": [
        {
          "rule": "required",
          "rawMessage": "الحقل name en مطلوب."
        }
      ],
      "examples": {
        "valid": [
          "example"
        ],
        "invalid": [
          "",
          null,
          null
        ]
      }
    },
    "is_all_day": {
      "field": "is_all_day",
      "required": true,
      "type": "string",
      "rules": [
        {
          "rule": "required",
          "rawMessage": "الحقل is all day مطلوب."
        }
      ],
      "examples": {
        "valid": [
          "example"
        ],
        "invalid": [
          "",
          null,
          null
        ]
      }
    },
    "rf_category_id": {
      "field": "rf_category_id",
      "required": true,
      "type": "string",
      "rules": [
        {
          "rule": "required",
          "rawMessage": "الحقل rf category id مطلوب."
        }
      ],
      "examples": {
        "valid": [
          "example"
        ],
        "invalid": [
          "",
          null,
          null
        ]
      }
    },
    "terms_and_conditions": {
      "field": "terms_and_conditions",
      "required": true,
      "type": "string",
      "rules": [
        {
          "rule": "required",
          "rawMessage": "شروط وأحكام الخدمة مطلوبة."
        }
      ],
      "examples": {
        "valid": [
          "example"
        ],
        "invalid": [
          "",
          null,
          null
        ]
      }
    },
    "icon": {
      "field": "icon",
      "required": false,
      "type": "string",
      "rules": [
        {
          "rule": "unknown",
          "rawMessage": "يرجى اختيار أيقونة لهذا التصنيف الفرعي للخدمة."
        }
      ],
      "examples": {
        "valid": [
          "example"
        ],
        "invalid": [
          "",
          null
        ]
      }
    }
  },
  "requiredFields": [
    "is_all_day",
    "name_ar",
    "name_en",
    "rf_category_id",
    "terms_and_conditions"
  ],
  "optionalFields": [
    "icon"
  ]
}