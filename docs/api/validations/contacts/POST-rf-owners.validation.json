{
  "endpoint": "rf/owners",
  "method": "POST",
  "fields": {
    "last_name": {
      "field": "last_name",
      "required": true,
      "type": "string",
      "rules": [
        {
          "rule": "invalid",
          "rawMessage": "صيغة الحقل اسم العائلة .غير صحيحة"
        },
        {
          "rule": "required",
          "rawMessage": "الحقل اسم العائلة مطلوب."
        }
      ],
      "examples": {
        "valid": [
          "example"
        ],
        "invalid": [
          "",
          null,
          null
        ]
      }
    },
    "first_name": {
      "field": "first_name",
      "required": true,
      "type": "string",
      "rules": [
        {
          "rule": "required",
          "rawMessage": "الحقل الاسم مطلوب."
        }
      ],
      "examples": {
        "valid": [
          "example"
        ],
        "invalid": [
          "",
          null,
          null
        ]
      }
    },
    "phone_country_code": {
      "field": "phone_country_code",
      "required": true,
      "type": "string",
      "rules": [
        {
          "rule": "required",
          "rawMessage": "الحقل phone country code مطلوب."
        }
      ],
      "examples": {
        "valid": [
          "example"
        ],
        "invalid": [
          "",
          null,
          null
        ]
      }
    },
    "phone_number": {
      "field": "phone_number",
      "required": true,
      "type": "string",
      "rules": [
        {
          "rule": "required",
          "rawMessage": "الحقل رقم الجوال مطلوب."
        }
      ],
      "examples": {
        "valid": [
          "example"
        ],
        "invalid": [
          "",
          null,
          null
        ]
      }
    }
  },
  "requiredFields": [
    "first_name",
    "last_name",
    "phone_country_code",
    "phone_number"
  ],
  "optionalFields": []
}