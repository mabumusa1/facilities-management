{
  "endpoint": "rf/sub-leases",
  "method": "POST",
  "fields": {
    "lease_id": {
      "field": "lease_id",
      "required": true,
      "type": "string",
      "rules": [
        {
          "rule": "required",
          "rawMessage": "الحقل lease id مطلوب."
        }
      ],
      "examples": {
        "valid": [
          "example"
        ],
        "invalid": [
          "",
          null,
          null
        ]
      }
    },
    "tenant_type": {
      "field": "tenant_type",
      "required": true,
      "type": "string",
      "rules": [
        {
          "rule": "required",
          "rawMessage": "الحقل نوع المستأجر مطلوب."
        }
      ],
      "examples": {
        "valid": [
          "example"
        ],
        "invalid": [
          "",
          null,
          null
        ]
      }
    }
  },
  "requiredFields": [
    "lease_id",
    "tenant_type"
  ],
  "optionalFields": []
}