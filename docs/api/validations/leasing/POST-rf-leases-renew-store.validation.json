{
  "endpoint": "rf/leases/renew/store",
  "method": "POST",
  "fields": {
    "rental_contract_type_id": {
      "field": "rental_contract_type_id",
      "required": true,
      "type": "string",
      "rules": [
        {
          "rule": "required",
          "rawMessage": "الحقل rental contract type id مطلوب."
        }
      ],
      "examples": {
        "valid": [
          "example"
        ],
        "invalid": [
          "",
          null,
          null
        ]
      }
    },
    "created_at": {
      "field": "created_at",
      "required": true,
      "type": "string",
      "rules": [
        {
          "rule": "required",
          "rawMessage": "الحقل created at مطلوب."
        }
      ],
      "examples": {
        "valid": [
          "example"
        ],
        "invalid": [
          "",
          null,
          null
        ]
      }
    },
    "start_date": {
      "field": "start_date",
      "required": true,
      "type": "string",
      "rules": [
        {
          "rule": "required",
          "rawMessage": "الحقل start date مطلوب."
        }
      ],
      "examples": {
        "valid": [
          "example"
        ],
        "invalid": [
          "",
          null,
          null
        ]
      }
    },
    "end_date": {
      "field": "end_date",
      "required": true,
      "type": "string",
      "rules": [
        {
          "rule": "required",
          "rawMessage": "الحقل end date مطلوب."
        }
      ],
      "examples": {
        "valid": [
          "example"
        ],
        "invalid": [
          "",
          null,
          null
        ]
      }
    },
    "rf_lease_id": {
      "field": "rf_lease_id",
      "required": true,
      "type": "string",
      "rules": [
        {
          "rule": "required",
          "rawMessage": "الحقل rf lease id مطلوب."
        }
      ],
      "examples": {
        "valid": [
          "example"
        ],
        "invalid": [
          "",
          null,
          null
        ]
      }
    },
    "number_of_years": {
      "field": "number_of_years",
      "required": false,
      "type": "string",
      "rules": [
        {
          "rule": "unknown",
          "rawMessage": "يرجى إضافة مدة الإيجار بالسنوات."
        }
      ],
      "examples": {
        "valid": [
          "example"
        ],
        "invalid": [
          "",
          null
        ]
      }
    },
    "number_of_months": {
      "field": "number_of_months",
      "required": false,
      "type": "string",
      "rules": [
        {
          "rule": "unknown",
          "rawMessage": "يرجى إضافة مدة الإيجار بالشهور."
        }
      ],
      "examples": {
        "valid": [
          "example"
        ],
        "invalid": [
          "",
          null
        ]
      }
    },
    "autoGenerateLeaseNumber": {
      "field": "autoGenerateLeaseNumber",
      "required": true,
      "type": "string",
      "rules": [
        {
          "rule": "required",
          "rawMessage": "الحقل auto generate lease number مطلوب."
        }
      ],
      "examples": {
        "valid": [
          "example"
        ],
        "invalid": [
          "",
          null,
          null
        ]
      }
    },
    "contract_number": {
      "field": "contract_number",
      "required": false,
      "type": "string",
      "rules": [
        {
          "rule": "unknown",
          "rawMessage": "يرجى إدخال رقم إيجار فريد."
        }
      ],
      "examples": {
        "valid": [
          "example"
        ],
        "invalid": [
          "",
          null
        ]
      }
    },
    "rental_type": {
      "field": "rental_type",
      "required": true,
      "type": "string",
      "rules": [
        {
          "rule": "required",
          "rawMessage": "الحقل rental type مطلوب."
        }
      ],
      "examples": {
        "valid": [
          "example"
        ],
        "invalid": [
          "",
          null,
          null
        ]
      }
    },
    "payment_schedule_id": {
      "field": "payment_schedule_id",
      "required": true,
      "type": "string",
      "rules": [
        {
          "rule": "required",
          "rawMessage": "الحقل payment schedule id مطلوب."
        }
      ],
      "examples": {
        "valid": [
          "example"
        ],
        "invalid": [
          "",
          null,
          null
        ]
      }
    },
    "units": {
      "field": "units",
      "required": true,
      "type": "string",
      "rules": [
        {
          "rule": "required",
          "rawMessage": "الحقل units مطلوب."
        }
      ],
      "examples": {
        "valid": [
          "example"
        ],
        "invalid": [
          "",
          null,
          null
        ]
      }
    },
    "units.0.rental_amount": {
      "field": "units.0.rental_amount",
      "required": true,
      "type": "string",
      "rules": [
        {
          "rule": "required",
          "rawMessage": "المبلغ السنوي للإيجار مطلوب"
        }
      ],
      "examples": {
        "valid": [
          "example"
        ],
        "invalid": [
          "",
          null,
          null
        ]
      }
    }
  },
  "requiredFields": [
    "autoGenerateLeaseNumber",
    "created_at",
    "end_date",
    "payment_schedule_id",
    "rental_contract_type_id",
    "rental_type",
    "rf_lease_id",
    "start_date",
    "units",
    "units.0.rental_amount"
  ],
  "optionalFields": [
    "contract_number",
    "number_of_months",
    "number_of_years"
  ]
}